import { View } from 'react-native';
import React from 'react';

import { Typography } from '@Src/reusable';

const LoginScreen = () => {
  return (
    <View>
      <Typography variant='h2'></Typography>
    </View>
  );
};

export default LoginScreen;
